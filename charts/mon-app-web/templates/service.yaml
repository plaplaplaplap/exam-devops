apiVersion: v1
kind: Service
metadata:
  name: mon-app-web
  labels:
    app: mon-app-web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: mon-app-web
